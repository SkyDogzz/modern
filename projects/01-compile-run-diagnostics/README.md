# Project 1 Starter

Compile directly:

```bash
c++ -std=c++23 -Wall -Wextra -Wpedantic -g main.cpp -o app
./app
```

Then create and repair the syntax and type errors required by Project 1. Record the
commands and diagnostics; do not commit the intentionally broken state to the
curriculum scaffold.
